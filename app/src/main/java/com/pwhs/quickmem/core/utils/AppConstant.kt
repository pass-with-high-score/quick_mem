package com.pwhs.quickmem.core.utils

import com.pwhs.quickmem.BuildConfig

object AppConstant {
    const val BASE_URL = BuildConfig.BASE_URL
    const val EMAIL_VERIFICATION_URL = BuildConfig.EMAIL_VERIFICATION_URL
    const val BANNER_ADS_ID = BuildConfig.BANNER_ADS_ID
    const val INTERSTITIAL_ADS_ID = BuildConfig.INTERSTITIAL_ADS_ID
    const val REWARD_ADS_ID = BuildConfig.REWARD_ADS_ID
    const val REWARDED_INTERSTITIAL_ADS_ID = BuildConfig.REWARDED_INTERSTITIAL_ADS_ID
}